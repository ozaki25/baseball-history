// 試合データの取得と勝敗分の計算処理\nconst yearGames = getGamesForYear(selectedYear); // 選択された年の試合データを取得\nconst totalGames = yearGames.length; // 試合数\nconst wins = yearGames.filter(game => game.result === 'win').length; // 勝ち数\nconst losses = yearGames.filter(game => game.result === 'loss').length; // 負け数\nconst draws = yearGames.filter(game => game.result === 'draw').length; // 引き分け数\n\n// 表示形式の更新\nconst displayText = `🏟️ ${selectedYear}年 (${totalGames}試合) (${wins}勝${losses}敗${draws}分)`;\n